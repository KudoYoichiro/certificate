<h1>Listing maps</h1>

<table>
  <thead>
    <tr>
      <th>Engineer</th>
      <th>Instrument</th>
      <th>Operation</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @maps.each do |map| %>
      <tr>
        <td><%= map.engineer %></td>
        <td><%= map.instrument %></td>
        <td><%= map.operation %></td>
        <td><%= map.status %></td>
        <td><%= link_to 'Show', map %></td>
        <td><%= link_to 'Edit', edit_map_path(map) %></td>
        <td><%= link_to 'Destroy', map, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Map', new_map_path %>
